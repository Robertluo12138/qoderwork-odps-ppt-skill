用 `odps-ppt-report-builder` 帮我做这次报告。

这台电脑不一定配得很全，所以先别急着出 PPT，先把基础检查做完。

你先看这几件事：

- 现在是不是 Qoder Work，不要按 Qoder IDE 来理解
- 当前系统是 Mac 还是 Windows，只写当前系统对应的步骤
- skill 文件夹有没有放对位置
- `assets/company_private_brief.md` 在不在
- `odpscmd` 能不能用
- 这台机器有没有 Python

如果有任何一项不通过，就先停在那一步，直接告诉我：

- 缺的是什么
- 我现在只需要做什么
- 做完以后我应该回你什么

除非确实有必要，不要让我额外装 Python、git、Node.js、pip 这些东西。  
如果机器上已经有 Python，再考虑走脚本模式；如果没有，就先按少安装模式做下去。

取数和出稿按下面的要求来：

- 先读 `assets/company_private_brief.md`
- 如果没有这个文件，就提醒我从模板复制，不要自己猜 SQL
- 取数只用私有说明里写好的 SQL 或命令
- 中间文件放到当前工作目录下的一个任务文件夹里
- 至少整理出 `raw_schema.txt`、`report_context.md`、`slide_plan.generated.json`
- 如果能直接生成 PPT，尽量用原生文本框、表格、图表和形状
- 不要整页截图，也不要把正文重点做成大贴图
- 如果当前机器还不能直接生成 `.pptx`，就先给完整讲稿，不要停住

如果我只说一句这种话：

- 帮我生成霸王茶姬三月的 PPT
- 帮我出霸王茶姬月报
- 帮我重跑上周周报

你就先按私有说明里的业务别名、默认模板和默认日期规则来理解。  
如果没写年份，默认当前年份，但要在回复里把这个假设写清楚。  
如果只缺一个关键信息，只问我一个短问题；如果信息已经够了，就直接开始。

这次的需求：

- 业务：`这里换成业务名`
- 时间范围：`这里换成时间范围`
- 报告类型：`这里换成周报 / 月报 / 复盘 / 经营分析`

最后回我这些：

- 最终文件路径
- 3 到 5 条重点结论
- 如果没跑通，明确告诉我卡在哪一步
